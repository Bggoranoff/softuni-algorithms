package Cobinatorial;
import java.util.*;

public class Cubes {
    public static void main(String[] args) {
        int[] arr = new int[12];
        Scanner s = new Scanner(System.in);
        arr = Arrays.stream(s.nextLine().split("\\s")).mapToInt(Integer::parseInt).toArray();

    }
}
